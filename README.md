# うちの子証明写真メーカー

ペットも人も証明写真風に仕上げられる、ブラウザ完結型の Web アプリ。

**写真データと生成結果はブラウザ内で処理され、サーバーには送信されません。**

## 主な機能

- **AI 背景除去**: [`@imgly/background-removal`](https://github.com/imgly/background-removal-js)（`isnet_quint8` モデル）をブラウザ内で実行
- **12種のサイズプリセット**: 履歴書 / マイナンバーカード（オンライン・印刷）/ 各種パスポート / 免許 / 医学・司法系試験 / 英検 S-CBT / 雇用保険 / 国際運転免許 / TOEIC 等
- **12色の背景**: スタンダードブルー、ホワイト、レッド、ピンク、オレンジ、グリーン、グレー系、チャコール、ブラック、ブルー、イエロー
- **単色 / 上→白グラデーション**を切替
- **直感的な操作**: ドラッグで位置調整、マウスホイール / ピンチで拡大縮小、左右反転
- **書き出し解像度プリセット**:
  - 印刷標準 (300 dpi)
  - プロ印刷 (350 dpi)
  - オンライン申請用 (長辺 640 px 目安)
- **保存 / 共有**: PNG ダウンロード、対応ブラウザでは OS の共有シートから共有・写真アプリ保存
- レスポンシブ UI（スマホ最適化）
- OGP / Twitter Card 用サムネイル設定済み

## 技術スタック

| 項目 | |
|---|---|
| フレームワーク | Vite + TypeScript（純 DOM / Canvas） |
| 背景除去 | `@imgly/background-removal` v1.7（AGPL-3.0、ONNX Runtime Web） |
| ホスト | 静的配信（任意） |

## 開発

```sh
pnpm install   # または npm install
pnpm dev
```

- 開発サーバーは `Cross-Origin-Opener-Policy` / `Cross-Origin-Embedder-Policy` ヘッダを送信し、WebAssembly のマルチスレッド実行（`crossOriginIsolated`）を有効化します（`vite.config.ts`）。
- AI モデル（数十 MB）は初回アクセス時に CDN からブラウザへダウンロードされ、以降はブラウザキャッシュから読み込まれます。**写真そのものは送信されません。**

```sh
pnpm build     # ./dist に静的ビルド
pnpm preview   # ビルド成果物をローカル確認
```

## プライバシー

- アップロードされた画像、推論結果、編集結果はいずれもブラウザ内で処理され、サーバー側に送信・保存されません。
- 通信が発生するのは、AI モデル（重み）の CDN 取得のみです。
- 本番環境にデプロイする際も、上記2つの HTTP レスポンスヘッダを付与してください（推奨: Cloudflare Pages `_headers` / Vercel `vercel.json`）。

例 (`public/_headers`):
```
/*
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Embedder-Policy: require-corp
```

## 生成画像の利用について（企業・商用）

**本アプリは、作成された画像に対して追加の利用制限を課しません。**

| 行為 | 可否 |
|---|---|
| 商用利用（広告、販促、受託制作等） | ✅ 可 |
| SNS 投稿（個人・企業アカウント問わず） | ✅ 可 |
| 社内資料、プレゼン、名簿等 | ✅ 可 |
| 二次加工・再配布（画像単体として） | ✅ 可 |
| 画像のクレジット表記 | 不要（任意） |
| 事前の申請・報告 | 不要 |

生成画像の著作権・肖像権・利用許諾は、元写真の権利関係に従います。本アプリは生成画像に対して権利を主張しません。第三者の写真やキャラクター、商標等を含む画像を使う場合は、利用者側で必要な権利確認を行ってください。

提出用の「本物の証明写真」として利用する場合は、提出先の公式要件を必ず確認してください。

## ライセンス

本リポジトリ（**アプリのソースコード**）は **AGPL-3.0-only** で配布されます。
これは依存ライブラリ [`@imgly/background-removal`](https://github.com/imgly/background-removal-js) が AGPL-3.0 でライセンスされているためです。AGPL-3.0 の条件に従い、本アプリをネットワーク経由で提供する場合は、利用者が対応するソースコードを取得できる手段を提供する必要があります。

**AGPL-3.0 が影響するのは、アプリのコードを再配布・改変ホストする事業者のみ**で、本アプリを利用して画像を作るエンドユーザーには影響しません。この切り分けは FSF の公式解釈と一致します（[AGPLv3 FAQ](https://www.gnu.org/licenses/gpl-faq.html#AGPLv3InteractingRemotely)）。

本番サイトでは、フッターとライセンス欄から以下へアクセスできるようにしています。

- 対応するソースコード: <https://github.com/nyanko3141592/Local-IdPhotoMaker>
- AGPL-3.0-only 本文: `/LICENSE`
- 依存ライブラリ通知: `/THIRD_PARTY_NOTICES.txt`

### 依存ライブラリのライセンス概要

| ライセンス | 件数 |
|---|---|
| AGPL-3.0-only | 1 (`@imgly/background-removal`) |
| MIT | 65 |
| BSD-3-Clause | 12 |
| Apache-2.0 | 3 |
| ISC | 2 |

いずれも AGPL-3.0 と両立可能な許諾型ライセンスです。詳細は `pnpm licenses` 相当のコマンドで再確認できます:

```sh
npx license-checker --production --summary
```

全文は [`LICENSE`](./LICENSE) を参照してください。

## 免責事項

- 生成画像が各提出先の公式要件（寸法、背景色、表情、服装、撮影日等）を完全に満たすことは保証されません。**必ず提出先の最新の仕様を利用者ご自身で確認してください。**
- 背景除去は AI による自動処理のため、髪の毛・眼鏡・輪郭などで誤検出が発生する場合があります。書き出し前に必ず仕上がりをご確認ください。
- 本アプリは娯楽・補助目的で提供されており、公的機関・検定機関・印刷事業者等とは一切関係ありません。
- 本アプリの利用により生じた直接・間接の損害について、提供者は一切の責任を負いません。

詳しくはアプリ画面下部の「利用規約」「免責事項」を参照してください。
